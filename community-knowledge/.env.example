# Postgres retrieval — reuse shared Supabase (community_knowledge schema).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Embedding edge function (384-d gte-small)
EMBED_FUNCTION_NAME=gte-small

# Public URL after deploy (MCP + connect instructions)
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Ingestion (optional)
SKOOL_CLI=skool
COMMUNITY_SLUG=claude
